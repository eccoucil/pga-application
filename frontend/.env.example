# =============================================================================
# PGA Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project settings: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Use either the publishable key (new format) or anon key (legacy format)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_your-key
# OR
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# URL of the FastAPI backend server
NEXT_PUBLIC_API_URL=http://localhost:8001
